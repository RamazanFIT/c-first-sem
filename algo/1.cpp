#include <bits/stdc++.h>

using namespace std;

int main()
{
    // list <int> jojo;
    // list <int> roma(jojo); // just copy of jojo
    // list <int> li1 = jojo; // just copy of list jojo

    // only iterator in list 

    // resize(skolko elementov ostavit) 
    // resize(skolko elementov ostavit, kakim znazeniem заполнить недост елементы)
    // auto it = jojo.begin();

    // jojo.assign(4, 9);
    // jojo.assign({1, 2, 3, 4, 5, 6, 7, 8,9});
    // jojo.assign(jojo.begin(), jojo.end()) меняет размер на ограничение итераторами
    // jojo.swap(roma); меняет контейнеры
    
    // list.emplace(position, value)

    // list.insert(pos, N, value)

    // jojo.insert(pos, ++roma.begin(), --roma.end());

    // auto it = jojo.cend(); const iterator 

    // list <int> jojo;

    // int q = 5;

    // jojo.emplace(jojo.end(), q);
    // // jojo.emplace(jojo.begin(), )
    // jojo.insert(jojo.begin(), 10, 3);
    // list <int> :: iterator it;
    // for(it = jojo.begin();it!=jojo.end();it++)
    // {
    //     cout << *it <<" ";
    // }
    



}