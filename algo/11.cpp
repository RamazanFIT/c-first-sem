#include <bits/stdc++.h>

using namespace std;

int main()
{
    // cout << sizeof(int) << endl;
    // cout << sizeof(8) << endl; // сколько байтов занимает эта переменная

    // cout << sizeof(unsigned long long int) << endl;

    // cout << sizeof(double) <<" " << sizeof(0.0);
    // A == &(A[0])
    // A[5] == *(A+5)
    // int x = 5;

    // int * y = &x;

    // *(y+5) = x;

    // for(int i=1;i<=3;i++) *(y+i) = int(i+1);
    // for(int i=0;i<=3;i++) cout << y[i];
    // for(int i=0;i<5;i++)
    // {
    //     // y[i] = i+1;
    //     *(y+i) = i+1;
    // }

    // for(int i=0;i<5;i++)
    // {
    //     cout << *(y+i) <<" ";
    // }
    // y[0] = 1;
    // cout << y << endl;
    // // cout << y[0];
    // y[1] = 2;
    // // y[3] = 3;
    // // y[4] = 4;
    // // y[5] = 5;
    // // y[6] = 6;
    // // y[7] = 7;
    // cout << y;'


    int x;
    string j;
    int h;
    // cout << &j;

    // cout << &j << ' ' << &x << endl;
    // *(&x+1) = 'A';
    // cout << j;

    cout << int(&x) << ' ' << int(&j) << ' ' <<  int(&h);
   
    // cout << &x + 1 <<" " << &y + 1 << ' ' << &z << ' ' << &f <<' ' << &g;
    // x = 1;
    // *(&x+1) = x;
    // *(&x+2) = x;
    // cout << x << ' ' << y <<  ' ' << z;


    


}