#include <bits/stdc++.h>

using namespace std;

int main()
{
    int q, a, x;
    cin >> q >> a >> x;
    int w[q][a];
    for(int i=0;i<q;i++)
    {
        for(int s=0;s<a;s++)
        {
            
        }
    }
}