#include <bits/stdc++.h>

using namespace std;

int main()
{
    int q, a;
    cin >> q >> a;
    int w[q][a];
    for(int i=0;i<q;i+=2)
    {
        for(int s=0;s<a;s++)
        {
            int
        }
    }
}