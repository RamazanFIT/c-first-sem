#include <bits/stdc++.h>

using namespace std;

int main()
{
    int q;
    cin >> q;
    map <int, int> jojo;
    
    int w[q];
    for(int i=0;i<q;i++)
    {
        cin >> w[i];
    }
    for(int i=0;i<q;i++)
    {
        for(int s=0;s<q;s++)
        {
            if(s==i) continue;
            if(i<s)
        }
    }
}