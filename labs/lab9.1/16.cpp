#include <bits/stdc++.h>
#define fr(q, a) for(int q=0;q<a;q++)
#define pr(i, s) pair<int, string>
#define F first
#define S second

using namespace std;

int main()
{
    int q;
    cin >> q;
    pr(i, s) jojo[q];
    if(q%2==0)
    {
        fr(i, q/2)
        {
            cin >> jojo[i].F;
            cin >> jojo[i].S;
            cin >> jojo[i+1].F;
            cin >> jojo[i+1].S;
            if(jojo[i].S.empty())
            {
                cout << "queue is empty" << endl;
            }
            if()
        }
    }
    
}