#include <bits/stdc++.h>
// WORKKKKKKKKKK
using namespace std;

// void haha (int a)
// {
//     cout << a <<" ";

// }
void haha(pair<int, int> jo)
{
    cout << jo.first <<" " << jo.second <<endl;
}



int main()
{
    // int q;
    // cin >> q;
    // set <int> jojo{1, 2, 3, 4, 5};
    // vector <int> roma;
    // for(auto it = jojo.begin();it!=jojo.end();it++)
    // {
    //     roma.insert(roma.end(), *it);
    // }
    // // cout << roma.size();
    // for_each(roma.begin(), roma.end(), haha);
    // map <int, int> jojo{pair<int, int>(1, 2), pair<int, int>(3, 4), pair<int, int>(5, 6)};
    // vector<pair<int, int>> roma;
    // for(auto it=jojo.begin();it!=jojo.end();it++)
    // {
    //     roma.emplace(roma.end(), pair<int, int>(it->first, it->second));
    // }
    // cout << roma.size();
    // for_each(roma.begin(), roma.end(), haha);
    // it is actually work 

}