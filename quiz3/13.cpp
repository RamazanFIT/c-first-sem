#include <bits/stdc++.h>

using namespace std;


int main()
{
    int q;
    cin >> q;
    string w[q];
    for(int i=0;i<q;i++)
    {
        cin >> w[i];
    }
    int a;
    cin >> a;
    string j[a];
    for(int i=0;i<a;i++)
    {
        cin >> j[i]
    }
}