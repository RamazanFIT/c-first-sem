#include <bits/stdc++.h>
using namespace std;

int main()
{
    string q;

    getline(cin, q);
    int z=0, x=0;
    string w[q.size()];
    for(int i=0;i<q.size();i++)
    {
        if(q[i] != ' ')
        {
            w[z][x]
        }
        x++;
        
    }

}